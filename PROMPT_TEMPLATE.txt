[SYSTEM / INITIAL PROMPT TO ATTACH WITH PROTOCOL_1to1.md AT THE START OF AN AI CODING SESSION]

Hello. I have attached the document `PROTOCOL_1to1.md`, which defines the binding rules and operational workflow for this project.

I have a series of PDF files from a course or textbook inside the `pdfs/` directory (or a single master PDF split into chapters).

The objective is to convert this material into an editorial-grade LaTeX project with strict 1:1 fidelity to the original document, modularized using the `subfiles` package, and ready for local compilation or Overleaf upload.

To initiate Batch 1:
1. Inspect the PDF files inside `pdfs/` and set up `main.tex` using the templates provided in `templates/`.
2. Strictly enforce the 5 Golden Rules of `PROTOCOL_1to1.md` (specifically: zero summarization or omissions, pure vector LaTeX mathematical formulas, zero colored `tcolorbox` backgrounds, surgical 300 DPI figure extraction via PyMuPDF, and zero AI signatures/watermarks).
3. Process the first two chapters sequentially, saving them as `tex/Chapter_01.tex` and `tex/Chapter_02.tex`.
4. After generating each chapter, run the local compiler (`pdflatex`) and execute `python scripts/qa_verify_fidelity.py` to certify zero lexical or structural omissions compared to the original PDF text.

Please confirm once you have analyzed the `pdfs/` directory and begin processing Batch 1 immediately.
